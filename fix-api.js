const fetch = require('node-fetch'); // or use built-in fetch if Node >= 18

async function run() {
  const token = "YOUR_TOKEN_HERE"; // Need to grab a valid token or just let me do it directly via curl using the network tab token from front-end
  console.log("Waiting to execute via endpoint...");
}
run();
